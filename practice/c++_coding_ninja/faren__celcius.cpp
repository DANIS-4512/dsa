#include <iostream>
using namespace std;
int tempConversion(int S, int E, int W)
{
    cout << "enter start value";
    cin >> S;
    cout << "enter end value";
    cin >> E;
    cout << "enter start value";
    cin >> W;
    int jump;
    for (int i = S; i <= E; i++)
    {

        jump = S + W;
    }
    jump++;
    cout << jump << endl;
}

int main()
{
    int s,e,w;
         tempConversion(s,e,w);

    return 0;
}